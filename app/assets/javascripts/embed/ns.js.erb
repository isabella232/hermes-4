/*
  ---

  The js embed namespace definition + other vars

  (c) IFAD 2015
  @author: Stefano Ceschi Berrini <stefano.ceschib@gmail.com>
  @license: see LICENSE.md

  ---
*/

!(function(w, h){
  'use strict';

  var s = document.querySelector('#hermes_script_embed');
  h = w.__hermes_embed = {};
  h.assets = {
    logo: "<%= asset_url('hermes-logo.png') %>"
  };
  h.host = "<%= compute_asset_host %>";
  h.site_ref = s ? s.dataset.ref : w.location.host;
  h.env = "<%= Rails.env %>";
  h.DOM = {} // shortcut to get HERMES dom nodes (+ cache)

})(this);