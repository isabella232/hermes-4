/*
  ---

  Load the hermes css when this file is executed (it generates on assets:precompile the string of the hermes embed css)

  (c) IFAD 2015
  @author: Stefano Ceschi Berrini <stefano.ceschib@gmail.com>
  @license: see LICENSE.md

  ---
*/

!(function(w, d){
  'use strict';

  var style = d.createElement('style');
  style.type = 'text/css';
  style.innerHTML = "<%= ActionView::Base.new.escape_javascript(File.read('app/assets/stylesheets/hermes.css').gsub("\n", '')) %>";

  (d.getElementsByTagName('head')[0] || d.documentElement).appendChild(style);

})(this, document);
