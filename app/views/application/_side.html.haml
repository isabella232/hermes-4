#sites-list
  %ul.list-group
    = render Site.by_user(current_user) if Site.by_user(current_user).any?
    %li.list-group-item
      = link_to '#new-site-modal', class: 'btn btn-block btn-primary', :'data-toggle' => 'modal' do
        %i.fa.fa-plus
        Add new site
    - if Site.by_user(current_user).any?
      %li.list-group-item
        = link_to '#', class: 'btn btn-block btn-primary', :'data-toggle' => 'modal' do
          %i.fa.fa-plus
          Add new general broadcast